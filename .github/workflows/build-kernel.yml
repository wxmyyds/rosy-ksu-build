name: Build rosy kernel with ReSukiSU

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Clone kernel source from GitHub
        run: |
          git clone https://github.com/baunilla/android_kernel_xiaomi_rosy.git \
            -b lineage-21.0 --depth=1 rosy

      - name: Integrate ReSukiSU
        working-directory: rosy
        run: |
          curl -LSs "https://raw.githubusercontent.com/ReSukiSU/ReSukiSU/main/kernel/setup.sh" | bash
          echo "CONFIG_KSU=y" >> arch/arm64/configs/rosy-perf_defconfig
          echo "CONFIG_KSU_MANUAL_HOOK=y" >> arch/arm64/configs/rosy-perf_defconfig
          # AOSP clang 不支持 -fstack-protector-strong
          echo "CONFIG_CC_STACKPROTECTOR_NONE=y" >> arch/arm64/configs/rosy-perf_defconfig

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y bc bison flex libssl-dev python3 \
            python3-pip cpio rsync device-tree-compiler

      - name: Setup AOSP clang toolchain
        run: |
          mkdir -p toolchain && cd toolchain
          git clone --depth=1 --filter=blob:none --sparse \
            https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86 \
            clang
          cd clang
          git sparse-checkout set clang-r498229b
          echo "CLANG_DIR=$(pwd)" >> $GITHUB_ENV
          cd ..
          git clone --depth=1 -q https://github.com/LineageOS/android_prebuilts_gcc_linux-x86_aarch64_aarch64-linux-android-4.9
          git clone --depth=1 -q https://github.com/LineageOS/android_prebuilts_gcc_linux-x86_arm_arm-linux-androideabi-4.9
          echo "TOOLCHAIN_DIR=$(pwd)" >> $GITHUB_ENV

      - name: Build kernel
        working-directory: rosy
        run: |
          export PATH="${{ env.CLANG_DIR }}/clang-r498229b/bin:${{ env.TOOLCHAIN_DIR }}/aarch64-linux-android-4.9/bin:${{ env.TOOLCHAIN_DIR }}/arm-linux-androideabi-4.9/bin:$PATH"
          export ARCH=arm64
          export CROSS_COMPILE=${{ env.TOOLCHAIN_DIR }}/aarch64-linux-android-4.9/bin/aarch64-linux-android-
          export CROSS_COMPILE_ARM32=${{ env.TOOLCHAIN_DIR }}/arm-linux-androideabi-4.9/bin/arm-linux-androideabi-
          export CC=clang
          export CLANG_TRIPLE=aarch64-linux-gnu-
          export AS=clang
          export LD=ld.lld
          export AR=llvm-ar
          export NM=llvm-nm
          export OBJCOPY=llvm-objcopy
          export OBJDUMP=llvm-objdump
          export STRIP=llvm-strip

          make rosy-perf_defconfig
          make -j$(nproc) Image.gz-dtb 2>&1 | tee build.log

      - name: Upload kernel
        uses: actions/upload-artifact@v4
        with:
          name: rosy-kernel-resukisu
          path: |
            rosy/arch/arm64/boot/Image.gz-dtb
            rosy/build.log