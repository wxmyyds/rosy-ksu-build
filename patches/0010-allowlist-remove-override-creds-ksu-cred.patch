--- a/drivers/kernelsu/policy/allowlist.c
+++ b/drivers/kernelsu/policy/allowlist.c
@@ -464,13 +464,11 @@ void do_persistent_allow_list(void *unused)
     loff_t off = 0;
     int i;
 
-    const struct cred *saved = override_creds(ksu_cred);
     fp = filp_open(KERNEL_SU_ALLOWLIST, O_WRONLY | O_CREAT | O_TRUNC, 0644);
     if (IS_ERR(fp)) {
         pr_err("save_allow_list create file failed: %ld\n", PTR_ERR(fp));
-        revert_creds(saved);
         return;
     }
 
     // store magic and version
@@ -486,7 +484,6 @@ void do_persistent_allow_list(void *unused)
     mutex_unlock(&allowlist_mutex);
 
 out:
-    revert_creds(saved);
     filp_close(fp, 0);
 }